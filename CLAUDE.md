# Echoes of Neon — read this first

Full context, architecture, active work, and the mandatory AI agent protocol all live
in **`MEMORY.md`** at this project's root. Read it before making any change here — it
has its own "READ FIRST" instruction and requires updating its Changelog and Task
Tracking tables after every task. This file exists only so that instruction gets
surfaced automatically at the start of a session, instead of depending on whoever's
working here remembering to open `MEMORY.md` manually.

If another AI tool (not just Claude) is also working on this project, point it at
`MEMORY.md` too — its protocol is written for "an AI agent," not specifically Claude.
